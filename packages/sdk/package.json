{
  "name": "@nexus/sdk",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@nexus/core": "workspace:*",
    "@nexus/bridge": "workspace:*",
    "@nexus/memory": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "Apache-2.0",
  "description": "@nexus/sdk — Nexus platform component",
  "repository": {
    "type": "git",
    "url": "https://github.com/nexus-ai/nexus.git",
    "directory": "packages/sdk"
  }
}
